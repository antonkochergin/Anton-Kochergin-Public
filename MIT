19.03.25
git init second-project //создаем второй проект 
mk der python
git add python/
python3 python/main.py
git add -A //добавляет все файлы , которые git видел до этого 
git checkout хеш_комита //старый но распр. для переключения на конскретные комиты
git log --oneline //указывает на состояние по определенной вестке
git checkout <faac> - показывает хеш
git checkout main - переключение на ветску main 
man git //мануал гита
git switch -d <хэш> //новый вариант переключения 
git switch -d main~1 // возращение на один commit назад
git switch - //возращает на то место, откуда пришли 
git switch -c <имя_новой_ветки> //Создание и переключение на новую ветку:
git revert <хэш> // отмена изменений из конкретного комитта 
git reset  //перенапрявляет ветку
git pull origin main//заргрузка из гитхаба изменения
git push -f origin main // принудительно загружает комиты , если ты на несколько комитов назад , удаляет все последующие комиты, которые были до этого
git commit --amend //добавить изменения к последнему комиту 
git blame <путь до файла > // выводит автора и дату создания изменений 
git branch <имя> // создание ветки
git checkout <имя> //переключение на другую ветку
git switch main // возращает на ветку main 
git push origin <имя вестки> // пушим в конкретную ветку коммит 
